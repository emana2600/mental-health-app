import { ProfilDirectiveDirective } from './profil-directive.directive';

describe('ProfilDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfilDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
